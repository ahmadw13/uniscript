[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "uniscript"
version = "0.1.1"
description = "Convert plain strings to Unicode subscript/superscript — no HTML, no LaTeX, just text."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
keywords = ["unicode", "subscript", "superscript", "text", "terminal"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Text Processing",
    "Topic :: Utilities",
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[project.scripts]
uniscript = "uniscript._cli:main"

[project.urls]
Homepage = "https://github.com/ahmadw13/uniscript"
Repository = "https://github.com/ahmadw13/uniscript"
Issues = "https://github.com/ahmadw13/uniscript/issues"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"

[tool.hatch.build.targets.wheel]
packages = ["uniscript"]
